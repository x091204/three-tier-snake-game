apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: three-tier-dev
spec:
  selector:
      app: frontend
  ports:
  - name: frontend-svc
    protocol: TCP
    port: 80
    targetPort: 80