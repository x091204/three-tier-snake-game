apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: three-tier-dev
  labels:
    monitoring: enabled
spec:
  selector:
    app: backend
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000