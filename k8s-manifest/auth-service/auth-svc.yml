apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: three-tier-dev
  labels:
    monitoring: enabled
spec:
  selector:
    app: auth
  ports:
  - name: http
    protocol: TCP
    port: 4000
    targetPort: 4000