apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-svc
  namespace: {{  .Values.namespace }}
spec:
  selector:
      app: frontend
  ports:
  - name: frontend-svc
    protocol: TCP
    port: 80
    targetPort: 80