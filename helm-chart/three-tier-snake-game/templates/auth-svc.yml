apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-auth-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: auth
spec:
  selector:
    app: auth
  ports:
  - name: http
    protocol: TCP
    port: 4000
    targetPort: 4000
